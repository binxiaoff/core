framework:
    assets:
        base_urls:
            - "%router.request_context.scheme%://%env(HOST_DEFAULT_URL)%"
        packages:
            gulp:
                base_path: /assets
        json_manifest_path: '%kernel.project_dir%/public/default/assets/rev-manifest.json'

swiftmailer:
    default_mailer: fast
    mailers:
        normal:
            transport: 'smtp'
            url: '%env(MAILER_URL)%'
        fast:
            transport: 'smtp'
            url: '%env(MAILER_URL)%'

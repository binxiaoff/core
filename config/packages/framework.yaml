framework:
    secret: '%env(APP_SECRET)%'
    csrf_protection: true
    http_method_override: true
    trusted_hosts: ~
    session:
        handler_id: ~
        cookie_samesite: lax
        cookie_domain: .kls-platform.com
    php_errors:
        log: true
    form:
        csrf_protection:
            field_name: _csrf

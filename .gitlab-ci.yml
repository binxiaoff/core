# Select what we should cache
cache:
    paths:
        - vendor/

before_script:
    - composer install

test:application:
    script:
        - php -dpcov.enabled=1  bin/phpunit --coverage-text --colors=never

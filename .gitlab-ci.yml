# Select what we should cache
cache:
    paths:
        - vendor/

before_script:
    - yarn install
    - npm run-script build
    - composer install

test:application:
    script:
        - bin/phpunit

<ul class="nav-main">
    {% for menu in navigation %}
        {% if menu.zone in userZones %}
            <li{% if menu.zone == activeMenu %} class="active"{% endif %}>
                <a{% if menu.children is not empty %} class="nav-submenu" data-toggle="nav-submenu"{% endif %}{% if menu.uri is not empty %} href="{{ adminUrl }}/{{ menu.uri }}"{% endif %}>
                    {{ menu.title }}
                </a>
                {% if menu.children is not empty %}
                    <ul>
                        {% for subMenu in menu.children %}
                            {% if subMenu.zone is not defined or subMenu.zone in userZones %}
                                <li><a href="{{ adminUrl }}/{{ subMenu.uri }}">{{ subMenu.title }}</a></li>
                            {% endif %}
                        {% endfor %}
                    </ul>
                {% endif %}
            </li>
        {% endif %}
    {% endfor %}
</ul>
